// 序列化对象
// 将 json 对象转换为 a=1&b=2 形式的字符串

function serializeData(data) {
  var uri = [];
  var key;
  for (key in data) {
    uri.push(encodeURIComponent(key) + '=' + encodeURIComponent(data[key]));
  }
  return uri.join('&');
}

export default serializeData;
